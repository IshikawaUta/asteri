name: Python Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install setuptools wheel build psutil gevent watchdog h2
        pip install -e .
        pip install flask fastapi tornado
        pip install ruff mypy types-psutil types-setuptools

    - name: Run Ruff Static Analysis
      run: |
        ruff check .

    - name: Run Mypy Type Checking
      run: |
        mypy asteri tests --ignore-missing-imports

    - name: Verify CLI Version
      run: |
        asteri --version

    - name: Run Unit Tests
      run: |
        python -m unittest discover -s tests --buffer

    - name: Run Full Regression Suite
      run: |
        chmod +x test_asteri_cli.sh
        ./test_asteri_cli.sh
